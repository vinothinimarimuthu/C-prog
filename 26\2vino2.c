#include<stdio.h>
int main()
{
int num,count=0;
printf("enter the n numbers:");
scanf("%d",&num);
while(num!=0)
{
digits=num%10;
num=num%10;
reverse=reverse*10+digits;
{
printf("\n%d",&reverse);
}
}
}
