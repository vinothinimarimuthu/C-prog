#include<stdio.h>
int main()
{
int temp,flag,num;
printf("\n enter the number:");
scanf("%d",&num);
temp=num
while(temp!=1)
{
if(temp2!=0)
{
flag=1;
break;
}
if(flag==0)
{
printf("\n is power of 2,num");
}
else if(flag==1)
{
printf("\n is power of not 2,num");
}
return 0;
}
}
