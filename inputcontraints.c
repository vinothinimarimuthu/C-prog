#include<stdio.h>
 void main()
{
    int a,b;
    int sum=0,i[60];
    int avg=0;
    scanf("%d",&b);
    for(a=0;a<n;a++)
    {
        scanf("%d",&a[i]);
    }
    for(a=0;a<n;a++)
    {
        sum=sum+a[i];
    }
avg=sum/n;
printf("%d",avg);
    
}
